"""Potation — the native Audible acquisition engine.

Replaces the LibationCli subprocess calls in `services/cli.py`, the direct reads
of Libation's SQLite in `services/libation.py`, and the LibationBridge C#
sidecar. Built up across phases; see the plan for what lands when.
"""
